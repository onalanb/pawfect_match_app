name: pawfect_match_app
description: "A Pawfect Match Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^0.8.3
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  go_router:
  path: any
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  sqflite_common_ffi:
  mockito: ^5.4.4
  build_runner: ^2.4.8

flutter:
  uses-material-design: true